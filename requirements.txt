# MCP (Model Context Protocol) Framework
fastmcp>=0.2.0      # FastMCP server framework for building MCP servers
mcp>=1.0.0          # MCP protocol library with JSON-RPC support

# HTTP Client
httpx>=0.27.0       # Async HTTP client with streaming support (used for SSE)
httpx-sse>=0.4.0    # Server-Sent Events support for httpx (for bi-directional communication)
